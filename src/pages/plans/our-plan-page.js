import Layout from "../../components/layout/layout";
import {Container, Typography} from "@mui/material";

const LikedTrailersPage = () => {
    return (
        <Layout>
            <Container>
                <Typography variant="h2" align="center">
                    we are going to connect you to your system => click the button below
                </Typography>
         <Typography variant="h2" align="center">
                    click to check soil water level and get current weather Updates
        Thanks and keep the Better dream to Africa
                </Typography>
            </Container>
        </Layout>
    )
}

export default LikedTrailersPage;
